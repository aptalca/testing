name: Testing

on:
  workflow_dispatch:

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.3
        with:
          fetch-depth: '0'
        
      - name: Testing
        run: |
          echo "BLAH=BLAHBLAH" >> $GITHUB_ENV
          echo ${{ env.BLAH }}

      - name: Testing 2
        run: |
          echo ${{ env.BLAH }}
