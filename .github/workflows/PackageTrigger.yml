name: Package Trigger

on:
  schedule:
    - cron:  '*/5 * * * *'

jobs:
  package-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Package Trigger
        run: |
          curl -X POST \
            https://ci.linuxserver.io/job/Tools/job/cleanup-X86-BUILDER-2/build \
            --user ${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_TOKEN }}
