name: Package Trigger

on:
  schedule:
    - cron:  '*/5 * * * *'

jobs:
  package-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Package Trigger
        run: |
          echo "running package trigger"
