name: Package Trigger

on:
  schedule:
    - cron:  '* * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Package Trigger
        run: |
          echo "running package trigger"
